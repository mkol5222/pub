# py-hello

Minimal Python CLI used to demonstrate:

```bash
uvx github:mkol5222/pub/py-hello hello
```

The project also exposes a direct `hello` script, so the equivalent explicit form is:

```bash
uvx --from github:mkol5222/pub/py-hello hello
```