# mcp-node-echo

Minimal stdio MCP server with one tool:

- `echo`: returns the provided input prefixed with `echo responds:`

The runnable binary is exposed from the repository root as `mcp-node-echo`.